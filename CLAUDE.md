@AGENTS.md

## Claude Code

Nested package/app rules live in `AGENTS.md` beside that tree. When editing under a package or app, read that directory's `AGENTS.md` (Cursor loads it by path; Claude Code may not). Investigation depth: `.agents/tradecraft.md`.
